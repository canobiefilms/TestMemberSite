<p>
  <%= comment.body %> <span class="text-muted"> by: <%= comment.user.email %></span>  <%= link_to '<i class="fa fa-trash-o" aria-hidden="true"></i>'.html_safe, [comment.article, comment],
    method: :delete,
    data: { confirm: 'Are you sure?'} %>
</p>